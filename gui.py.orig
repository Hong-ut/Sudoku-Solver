import tkinter as tk
#from SUDOKU import board

from tkinter.ttk import Style
window = tk.Tk()
window.title('Sudoku')
window.geometry('270x330')
style = Style()
for r in range(9):
    for c in range(9):
        button = tk.Button(window, text="0", width=2, height=1, padx=3, pady=3, command=window.destroy)
        button.grid(row = r, column = c)

window.mainloop()
